# Hermes Harness Control Plugin

A Hermes-native tool plugin for controlling ACP-compatible coding harnesses through ACPX.

The first implementation is being validated as a disposable spike on the `spike/acpx-tool-plugin` branch.
